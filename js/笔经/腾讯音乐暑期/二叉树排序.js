//小红拿到了一个二叉树，每个节点的权值都不相同。小红每次操作可以交换任意两个节点的权值，她希望用尽可能少的操作，
//使得二叉树先序遍历序列为一个升序的数组。二叉树节点数量保证不超过10^5
class TreeNode{
    val
    left
    right
    constructor(val,left,right){
        this.val = (val === undefined? 0 : val)
        this.left = (left === undefined? null : left)
        this.right = (right === undefined? null : right)
    }
}
export function cntOfSwap(root){
    
}