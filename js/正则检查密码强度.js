// 密码规则：6-12位，必须包含数字，大写字母和特殊字符
let reg = /^(?=.*[0-9])(?=.*[A-Z])(?=.*[!@#$%^&*?])[0-9A-Za-z!@#$%^&*?]{6,12}$/
// 正则前瞻用于检查字符串是否包含满足条件子串，指针不移动
console.log(reg.test('123456Abc#')) // true
//如果位数不做限制，最后应该是.*$/，如果位数有限制，最后应该是{m,n}$，m和n是位数的范围